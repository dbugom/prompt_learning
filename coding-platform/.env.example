# Database Configuration
POSTGRES_DB=coding_platform
POSTGRES_USER=platform_user
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_123

# Redis Configuration
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD_456

# Backend Configuration
SECRET_KEY=CHANGE_THIS_SECRET_KEY_TO_A_RANDOM_64_CHAR_STRING
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
RATE_LIMIT_PER_MINUTE=10

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost

# Production Configuration (for deployment)
# NEXT_PUBLIC_API_URL=https://yourdomain.com/api
# NEXT_PUBLIC_WS_URL=wss://yourdomain.com/ws
# ALLOWED_ORIGINS=https://yourdomain.com

# Email Configuration (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Monitoring (optional)
SENTRY_DSN=

# Hetzner Configuration (for automated provisioning)
HETZNER_API_TOKEN=
HETZNER_SERVER_NAME=coding-platform-prod
HETZNER_SERVER_TYPE=cx32
HETZNER_LOCATION=nbg1
HETZNER_IMAGE=ubuntu-22.04

# Domain Configuration
DOMAIN=yourdomain.com
ADMIN_EMAIL=admin@yourdomain.com
